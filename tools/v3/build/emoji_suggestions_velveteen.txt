# CLDR emoji suggestions — REVIEW BEFORE USE, never auto-apply.
# Keep a line by adding  "<lemma>": "<emoji>"  to a JSON map for gloss3.py --emoji-map.
# Empty beats bad: when in doubt, leave the lemma out.

